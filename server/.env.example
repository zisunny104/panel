# ========================================
# 環境變數範例檔案
# ========================================
# 複製此檔案為 .env 並填入你的配置
# 注意：.env 檔案不會被上傳到 GitHub（已在 .gitignore 中排除）

# 伺服器環境
NODE_ENV=production

# 伺服器 Port（固定）
PORT=7645

# CORS 設定（根據你的網域調整）
# 測試環境使用 localhost
CORS_ORIGIN=http://localhost:7645
# 生產環境範例（請改成你的實際網域）
# CORS_ORIGIN=https://yourdomain.com:7645

# 資料庫路徑（相對於 server 目錄）
DB_PATH=../runtime/database/experiment.db

# 工作階段超時（秒）
SESSION_TIMEOUT=1800

# 分享代碼超時（秒）
SHARE_CODE_TIMEOUT=300

# WebSocket 心跳間隔（毫秒）
WS_HEARTBEAT_INTERVAL=30000

# WebSocket 心跳超時（毫秒）
WS_HEARTBEAT_TIMEOUT=60000

# WebSocket Rate Limit
WS_RATE_LIMIT_CAPACITY=20
WS_RATE_LIMIT_REFILL_PER_SEC=10
WS_RATE_LIMIT_VIOLATION_THRESHOLD=5

# WebSocket 低頻 Session 驗證排程（毫秒，預設 5 分鐘）
WS_SESSION_VALIDATION_INTERVAL=300000
