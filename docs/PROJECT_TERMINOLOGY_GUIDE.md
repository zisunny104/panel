# 專案用語指南

## 概述

技術術語保持英文，中文說明使用台灣習慣用語，避免中國或其他地區用語。

## 基本規則

### 技術術語

| 歧異用語                         | 專案用語                               | 適用場景     | 範例         |
| -------------------------------- | -------------------------------------- | ------------ | ------------ |
| 索引資料庫、網路通訊端、爪哇腳本 | `IndexedDB`、`WebSocket`、`JavaScript` | 程式設計術語 | 保持英文原文 |

### 在地認可用語

| 術語                                   | 狀態         | 說明                                          |
| -------------------------------------- | ------------ | --------------------------------------------- |
| **響應式設計 (Responsive Web Design)** | **在地認可** | 台灣 UI/UX 領域之標準慣用語，教科書與業界通用 |
| **自適應 (Adaptive)**                  | **具歧異性** | 台灣較常用於演算法領域，UI 設計較少使用       |

### 常見修正

#### 數量詞

| 歧異用語 | 專案用語 | 適用場景 | 範例         |
| -------- | -------- | -------- | ------------ |
| `批量`   | `批次`   | 資料處理 | 批次處理資料 |

#### 電腦/技術含義

| 歧異用語 | 專案用語 | 適用場景 | 範例                 |
| -------- | -------- | -------- | -------------------- |
| `本地`   | `本機`   | 電腦系統 | 本機儲存、本機資料庫 |

#### 地理/時區含義

| 歧異用語 | 專案用語 | 適用場景 | 範例               |
| -------- | -------- | -------- | ------------------ |
| `本機`   | `本地`   | 地理位置 | 本地時間、本地語言 |

#### 視覺/UI 相關

| 歧異用語                              | 專案用語 | 適用場景           | 範例                                                                                        |
| ------------------------------------- | -------- | ------------------ | ------------------------------------------------------------------------------------------- |
| `創建`                                | `建立`   | 操作/功能          | 建立容器、建立連線                                                                          |
| `圖像`                                | `圖片`   | UI 顯示/資源       | QR 圖片、圖片容器                                                                           |
| `當前`                                | `目前`   | 狀態/進度          | 目前角色、目前頁面                                                                          |
| `頭部`                                | `頂部`   | 位置 / 方位        | 新增卡片到列表頂部、面板頂部空間                                                            |
| `標頭` / `頭部` (作為 UI header 區塊) | `標題欄` | UI 元件 / 標題區塊 | 掃描器標題欄、模態視窗標題欄（註：HTTP 標頭請保留 `標頭` 一詞表示 request/response header） |

## 系統化用詞對照表

### 使用者介面與操作

| 歧異用語 | 專案用語 | 適用場景           | 範例                    |
| -------- | -------- | ------------------ | ----------------------- |
| `用戶`   | `使用者` | 所有用戶相關說明   | 使用者操作 → 使用者介面 |
| `獲取`   | `取得`   | API 說明、資料取得 | 取得工作階段資訊        |

### 技術術語

| 歧異用語   | 專案用語   | 適用場景      | 範例                   |
| ---------- | ---------- | ------------- | ---------------------- |
| `埠口`     | `連接埠`   | 網路設定      | 伺服器連接埠設定       |
| `端口`     | `連接埠`   | 網路設定      | 連接埠配置             |
| `設定文件` | `設定檔案` | 檔案說明      | 設定檔案目錄           |
| `實驗Hub`  | `實驗中樞` | 系統組件      | 實驗中樞管理器         |
| `訪問`     | `存取`     | 檔案系統      | 存取檔案系統           |
| `返回`     | `回傳`     | API 回應      | 回傳檔案列表           |
| `QR 碼`    | `QR Code`  | UI 功能       | QR Code 分享機制       |
| `共享`     | `共用`     | 資源分配      | 共用記憶體             |
| `加載`     | `載入`     | 檔案/資源讀取 | 載入設定檔案、載入媒體 |
| `常量`     | `常數`     | 程式設計      | 角色文字常數、狀態常數 |
| `添加`     | `新增`     | 操作/功能     | 新增工作階段、新增裝置 |

### 檔案與儲存

| 歧異用語 | 專案用語 | 適用場景 | 範例                 |
| -------- | -------- | -------- | -------------------- |
| `文件`   | `檔案`   | 檔案類型 | JSONL 檔案、設定檔案 |
| `文檔`   | `文件`   | 文件類型 | 架構文件、技術文件   |
| `文本`   | `文字`   | UI 顯示  | 角色文字、狀態文字   |

### 角色與同步用語

| 歧異用語               | 專案用語   | 適用場景       | 範例                 | 說明                                     |
| ---------------------- | ---------- | -------------- | -------------------- | ---------------------------------------- |
| `查看者 / 觀看者`      | `檢視者`   | 使用者身份     | 檢視者角色           | 只讀存取權限的使用者身份                 |
| `操作員 / 控制者`      | `操作者`   | 使用者身份     | 操作者角色           | 具有編輯/修改權限的使用者身份            |
| `僅查看 / 僅讀`        | `僅檢視`   | 連線狀態       | 連線狀態：僅檢視     | 檢視者的連線狀態文字                     |
| `同步進行中 / 操作中`  | `同步中`   | 連線狀態       | 連線狀態：同步中     | 操作者的連線狀態文字，表示數據同步進行中 |
| `未連接 / 離線 / 掉線` | `未連線`   | 連線狀態       | 連線狀態：未連線     | 未連接至工作階段時的狀態文字             |
| `伺服器下線 / 斷線`    | `已離線`   | 伺服器狀態     | 伺服器已離線         | 伺服器離線時的狀態文字                   |
| `用戶 / 客戶端`        | `裝置`     | 工作階段參與者 | 工作階段中的連接裝置 | 指代連接至工作階段的電腦/行動裝置        |
| `會話 / 連線`          | `工作階段` | 同步範圍       | 建立新工作階段       | 多裝置同步的邏輯分組單位                 |

### 頁面清單用語

| 歧異用語        | 專案用語          | 適用場景 | 範例           | 說明                           |
| --------------- | ----------------- | -------- | -------------- | ------------------------------ |
| `主頁 / 控制台` | `機台面板`        | 頁面名稱 | 切換至機台面板 | 系統主要操作介面，用於機台控制 |
| `實驗 / 測試頁` | `實驗管理`        | 頁面名稱 | 進入實驗管理   | 實驗數據收集和管理頁面         |
| `首頁 / 根頁面` | `index.html`      | 頁面路徑 | 頁面路徑設定   | 機台面板的 HTML 檔案路徑       |
| `實驗頁 / 測試` | `experiment.html` | 頁面路徑 | 頁面路徑設定   | 實驗管理的 HTML 檔案路徑       |

### 狀態與操作

| 歧異用語 | 專案用語 | 適用場景     | 範例                |
| -------- | -------- | ------------ | ------------------- |
| `活躍`   | `活動中` | 工作階段狀態 | 活動中的工作階段    |
| `不活躍` | `閒置中` | 工作階段狀態 | 閒置中的工作階段    |
| `非活躍` | `閒置中` | 工作階段狀態 | 閒置中的工作階段    |
| `緩存`   | `快取`   | 資料快取     | 離線佇列暫存        |
| `持久化` | `持久性` | 資料持久化   | 資料持久性          |
| `隊列`   | `佇列`   | 程式設計術語 | 離線佇列、訊息佇列  |
| `註銷`   | `移除`   | 連線管理     | 移除 WebSocket 連線 |
| `卸載`   | `移除`   | 資源釋放     | 移除事件監聽器      |
| `銷毀`   | `銷毀`   | 物件生命週期 | 銷毀物件實例        |

### 連線與資源管理

| 歧異用語     | 專案用語     | 適用場景       | 範例                | 說明                             |
| ------------ | ------------ | -------------- | ------------------- | -------------------------------- |
| `註銷連線`   | `移除連線`   | WebSocket 管理 | 移除 WebSocket 連線 | "註銷"為中國用語，台灣慣用"移除" |
| `卸載監聽器` | `移除監聽器` | 事件管理       | 移除事件監聽器      | 保持用詞一致性                   |
| `註冊連線`   | `註冊連線`   | WebSocket 管理 | 註冊新連線          | "註冊"為台灣慣用語，保留         |
| `登記裝置`   | `註冊裝置`   | 裝置管理       | 註冊新裝置          | 使用"註冊"而非"登記"             |
| `銷毀實例`   | `銷毀實例`   | 物件管理       | 銷毀管理器實例      | "銷毀"為技術術語，保留           |
| `地址`       | `網址`       | 網路位置       | 伺服器網址、API網址 | 避免與地理地址混淆               |

## 應用實例

| 歧異用語               | 專案用語                 | 適用場景   | 範例                       |
| ---------------------- | ------------------------ | ---------- | -------------------------- |
| `本地 IndexedDB 存儲`  | `本機 IndexedDB 儲存`    | 程式碼註解 | `// 本機 IndexedDB 儲存`   |
| `批量處理資料`         | `批次處理資料`           | 程式碼註解 | `// 批次處理資料`          |
| `實驗ID僅存本地`       | `實驗ID僅存本機`         | 程式碼註解 | `// 實驗ID僅存本機`        |
| `用戶操作流程`         | `使用者操作流程`         | 程式碼註解 | `// 使用者操作流程`        |
| `獲取伺服器資料`       | `取得伺服器資料`         | 程式碼註解 | `// 取得伺服器資料`        |
| `註銷 WebSocket 連線`  | `移除 WebSocket 連線`    | 日誌訊息   | 「移除 WebSocket 連線」    |
| `卸載事件監聽器`       | `移除事件監聽器`         | 日誌訊息   | 「移除事件監聽器」         |
| `本地目錄不存在`       | `本機目錄不存在`         | 錯誤訊息   | 顯示「本機目錄不存在」     |
| `埠口連線失敗`         | `連接埠連線失敗`         | 錯誤訊息   | 顯示「連接埠連線失敗」     |
| `端口設定錯誤`         | `連接埠設定錯誤`         | 錯誤訊息   | 顯示「連接埠設定錯誤」     |
| `本地實驗繼續進行`     | `本機實驗繼續進行`       | 日誌訊息   | 「本機實驗繼續進行」       |
| `活躍工作階段已中斷`   | `活動中的工作階段已中斷` | 日誌訊息   | 「活動中的工作階段已中斷」 |
| `離線隊列暫存`         | `離線佇列暫存`           | 日誌訊息   | 「離線佇列暫存」           |
| `狀態持久化`           | `資料持久性`             | 日誌訊息   | 「資料持久性」             |
| `獲取所有活躍工作階段` | `取得所有活躍工作階段`   | API 說明   | 取得所有活躍工作階段       |
| `設定文件加載失敗`     | `設定檔案載入失敗`       | API 說明   | 設定檔案載入失敗           |
| `訪問檔案系統`         | `存取檔案系統`           | API 說明   | 存取檔案系統               |
| `返回檔案列表`         | `回傳檔案列表`           | API 說明   | 回傳檔案列表               |
| `QR 碼分享`            | ` QR Code  分享`         | UI 功能    | QR Code 分享機制           |
| `共享記憶體`           | `共用記憶體`             | 程式設計   | 共用記憶體配置             |
| `加載設定檔案`         | `載入設定檔案`           | 日誌訊息   | 「載入設定檔案成功」       |
| `加載媒體失敗`         | `載入媒體失敗`           | 日誌訊息   | 「載入媒體失敗」           |
| `綁定地址`             | `綁定網址`               | 伺服器設定 | 伺服器綁定網址             |
| `存取地址`             | `存取網址`               | 用戶說明   | 存取網址設定               |

### 特殊用語說明

| 歧異用語 | 專案用語                | 適用場景           | 說明                                                                                                        |
| -------- | ----------------------- | ------------------ | ----------------------------------------------------------------------------------------------------------- |
| `排查`   | **故障排除 / 釐清問題** | 系統/硬體/服務問題 | 「排查」偏向中國大陸用語，建議使用台灣慣用的「故障排除」（針對系統/硬體）或「釐清問題」（針對找原因過程）。 |

## 特別說明：共享 vs 共用

- **共享 (Sharing)**：UI 功能，分享資料、分享連結等
  - 範例： QR Code 共享 → QR Code 分享

- **共用 (Shared)**：程式資源，共用記憶體、共用資源等
  - 範例：共享記憶體 → 共用記憶體

## Emoji 排除清單

以下位置保留 Emoji，**不進行移除**：

### 程式碼中的 Emoji

| 位置                              | Emoji | 用途               | 理由                                   |
| --------------------------------- | ----- | ------------------ | -------------------------------------- |
| panel-experiment-manager.js L2442 | 📺    | 無媒體內容提示圖示 | 視覺化介面元素，提升使用者體驗         |
| panel-power-control.js L402, 407  | 🚨    | 緊急停止警告符號   | 警示符號，具有明確的安全意義           |
| sync-confirm-dialog.js L47, 130   | 🔍    | 分享代碼驗證狀態   | UI 視覺反饋，配合文字「驗證中...」使用 |
| experiment.html L65, 72, 76       | ▶ ⏸ ⏹ | 播放器控制按鈕     | 實驗播放、暫停、停止按鈕的視覺符號     |
| experiment-page-manager.js        | ▶ ⏸   | 播放器狀態提示     | 動態顯示暫停/繼續狀態的視覺反饋        |
| panel-experiment-manager.js L416  | ⏸     | 暫停狀態指示       | 面板中暫停狀態的視覺標示               |

**原則**：保留具有實際視覺或功能意義的 Emoji，移除純裝飾性 Emoji。
